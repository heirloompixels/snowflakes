%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:parks, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:parks, :new)

.tabs-content
  -form_for :park, url(:parks, :create), :class => 'form-horizontal' do |f|
    =partial 'parks/form', :locals => { :f => f }
