%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:reports, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:reports, :new)

.tabs-content
  -form_for :report, url(:reports, :create), :class => 'form-horizontal' do |f|
    =partial 'reports/form', :locals => { :f => f }

 
